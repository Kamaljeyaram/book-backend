Notes:
- The app reads DATA_FILE from process.env.DATA_FILE or defaults to ./data/books.json
- Exported app in src/app.js allows tests to require without starting a server
- To run tests: npm install then npm test
